<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.intuso.housemate</groupId>
        <artifactId>comms-transport-rest</artifactId>
        <version>1.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>comms-transport-rest-server</artifactId>
    <packaging>jar</packaging>
    <name>Housemate Comms Transport REST Server</name>
    <description>Server for REST transport between Housemate components</description>

    <dependencies>

        <!-- Housemate API deps -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>plugin-api</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>object-proxy-simple</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Housemate JSON serialisation -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>comms-serialiser-json</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Guice stuff -->
        <dependency>
            <groupId>com.google.inject.extensions</groupId>
            <artifactId>guice-multibindings</artifactId>
            <version>${guice.version}</version>
        </dependency>

        <!-- Jetty server stuff -->
        <dependency>
            <groupId>org.eclipse.jetty</groupId>
            <artifactId>jetty-server</artifactId>
            <version>8.1.9.v20130131</version>
        </dependency>
        <dependency>
            <groupId>org.eclipse.jetty</groupId>
            <artifactId>jetty-servlet</artifactId>
            <version>8.1.9.v20130131</version>
        </dependency>
        <dependency>
            <groupId>org.glassfish.jersey.containers</groupId>
            <artifactId>jersey-container-servlet</artifactId>
            <version>2.2</version>
        </dependency>
    </dependencies>

</project>