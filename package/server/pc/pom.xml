<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.intuso.housemate</groupId>
        <artifactId>package-server</artifactId>
        <version>1.1.0-SNAPSHOT</version>
    </parent>

	<artifactId>package-server-pc</artifactId>
    <packaging>jar</packaging>
    <name>Housemate Package Server PC</name>
	<description>Deployable package for running the Housemate server on a PC</description>

    <properties>
        <jetty.version>8.1.7.v20120910</jetty.version>
    </properties>

    <dependencies>

        <!-- Server code module -->
		<dependency>
            <groupId>${project.groupId}</groupId>
			<artifactId>server</artifactId>
			<version>${project.version}</version>
		</dependency>

        <!-- Properties, log etc modules -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>platform-pc</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Transport implementations -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>comms-transport-socket-server</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>comms-transport-rest-server</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Serialisers -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>comms-serialiser-javabin</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>comms-serialiser-json</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Persistence implementation -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>persistence-flatfile</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Depend on web so it gets built before we add it to the package -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>web</artifactId>
            <version>${project.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>com.google.gwt</groupId>
                    <artifactId>gwt-user</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- Jetty server/web container stuff -->
        <dependency>
            <groupId>org.eclipse.jetty</groupId>
            <artifactId>jetty-server</artifactId>
            <version>${jetty.version}</version>
        </dependency>
        <dependency>
            <groupId>org.eclipse.jetty</groupId>
            <artifactId>jetty-webapp</artifactId>
            <version>${jetty.version}</version>
        </dependency>
        <dependency>
            <groupId>org.eclipse.jetty</groupId>
            <artifactId>jetty-io</artifactId>
            <version>${jetty.version}</version>
        </dependency>
        <dependency>
            <groupId>org.eclipse.jetty</groupId>
            <artifactId>jetty-servlet</artifactId>
            <version>${jetty.version}</version>
        </dependency>
        <dependency>
            <groupId>org.eclipse.jetty</groupId>
            <artifactId>jetty-util</artifactId>
            <version>${jetty.version}</version>
        </dependency>

        <!-- JNI libraries that need to be loaded globally so they can be shared -->
        <dependency>
            <groupId>org.scream3r</groupId>
            <artifactId>jssc</artifactId>
            <version>2.8.0</version>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>comms-transport-socket-client</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

	<build>
		<plugins>
            <!-- Package all dependencies into the one jar and make it executable -->
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <configuration>
                    <descriptorRefs>
                        <descriptorRef>jar-with-dependencies</descriptorRef>
                    </descriptorRefs>
                    <archive>
                        <manifest>
                            <addClasspath>true</addClasspath>
                            <mainClass>com.intuso.housemate.pkg.server.pc.Main</mainClass>
                        </manifest>
                    </archive>
                </configuration>
                <executions>
                    <execution>
                        <id>make-my-jar-with-dependencies</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
		</plugins>
	</build>
</project>
